export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  COMPONENTS: '/components',
} as const;

export const API_ROUTES = {
  AUTH_LOGIN: '/api/auth/login',
  HELLO: '/api/hello',
} as const;
